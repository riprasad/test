name: Test Pull Request
on:
  pull_request:
    branches: [master]

jobs:
  verify-build:
    name: Verify Build
    runs-on: ubuntu-18.04    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v2
      - name: Build
        run: mvn clean install